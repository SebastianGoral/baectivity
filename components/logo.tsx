import React from "react";
import styled from "@emotion/styled";

const Wrapper = styled.span`
  font-family: "Alfa Slab One", cursive;
  margin: 0 auto;
  font-size: 50px;

  color: rgb(101, 48, 4);
`;

export const Logo = () => {
  return <Wrapper>baectivity</Wrapper>;
};
